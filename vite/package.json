{
  "scripts": {
    "setup": "yarn && yarn --cwd backend && yarn --cwd frontend",
    "start": "run-p start:*",
    "start:backend": "yarn --cwd backend start",
    "start:frontend": "yarn --cwd frontend dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
